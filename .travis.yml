language: python
matrix:
  include:
    - dist: xenial
python:
  - "3.8.0"
install:
  - pip install -r requirements.txt
script:
  - python3 -m unittest -v tests.test_moves
  - python3 -m unittest -v tests.test_board